import { TipoCentroCostoPipe } from './tipo-centro-costo.pipe';

describe('TipoCentroCostoPipe', () => {
	it('create an instance', () => {
		const pipe = new TipoCentroCostoPipe();
		expect(pipe).toBeTruthy();
	});
});
